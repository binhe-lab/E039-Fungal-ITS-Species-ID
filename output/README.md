# output

This folder stores BLAST results from query sequences.

Output files are named using the same base name as the input tarball, with a date prefix.

Example:

```text
20260504-yeast_ITS_results.blast.tsv
```
